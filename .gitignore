# ── Rust 构建产物 ───────────────────────────────────────────────────
# Cargo.lock **要进仓库**（这是二进制项目，锁版本才有可复现构建）
target/

# ── npm 包（packages/rsi3d-cli）────────────────────────────────────
node_modules/
*.tgz
npm-debug.log*
# 按需下载到包内的预编译二进制（平台矩阵产物，不该进仓库）
packages/*/vendor/

# ── 内置模板（scaffolds/）───────────────────────────────────────────
# `scaffolds/` 本身**要进仓库**（内置模板源就是被分发出去的产物），
# 只忽略它与产物目录里可能的构建残留
scaffolds/**/node_modules/

# ── 派生出来的契约产物（contract/）──────────────────────────────────
# **要进仓库**（别加进来）：它是"缓存"，但门禁 `contract --check` 与
# `committed_artifacts_match_the_source` 靠"重新生成 == 已提交"来抓漂移；
# 忽略掉它，那道门禁就永远绿，等于没有。
# 重新生成：rsi3d-harness contract --out contract

# ── 本地用户文件 ────────────────────────────────────────────────────
# 外部模板与插件默认在 ~/.rsi3d-harness/（仓库外）；万一有人在本目录里试，
# 也别让它们混进提交
.*-local/
*.rsi3d-local.json

# ── 系统与编辑器 ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.idea/
